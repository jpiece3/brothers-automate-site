---
---

<section class="hero" id="hero">
  <div class="container">
    <div class="hero-content">
      <span class="badge" data-animate="fade-up">AI Automations for Service Businesses</span>
      <h1 data-animate="fade-up">You Started a Business to Do<br /><span class="text-gradient">the Thing You're Good At</span></h1>
      <p class="hero-subheadline" data-animate="fade-up">
        We build AI systems that handle the rest: lead follow-up, ad creative, hiring, reviews, routing. So you can get back to it.
      </p>

      <div class="hero-cta" data-animate="fade-up">
        <a href="/projects" class="btn btn-primary btn-lg">See Our Projects</a>
        <a
          href="#"
          class="btn btn-secondary btn-lg"
          data-cal-link="james-pinder-simpleflo/free-quiz-audit-automation-brothers"
          data-cal-namespace="free-quiz-audit-automation-brothers"
          data-cal-config='{"layout":"month_view","useSlotsViewOnSmallScreen":"true"}'
        >Talk to the Brothers</a>
      </div>

      <div class="stats-bar" data-animate-stagger>
        <div class="stat">
          <span class="stat-number">1-3 Wks</span>
          <span class="stat-label">From Kickoff to Live</span>
        </div>
        <div class="stat">
          <span class="stat-number">100%</span>
          <span class="stat-label">Custom-Built</span>
        </div>
        <div class="stat">
          <span class="stat-number">2</span>
          <span class="stat-label">Brothers, Zero Middlemen</span>
        </div>
        <div class="stat">
          <span class="stat-number">24/7</span>
          <span class="stat-label">Runs Automatically</span>
        </div>
      </div>
      <p class="trust-signal" data-animate="fade-in">Two brothers who ran a service business for 5 years. Now we build the systems we wish we'd had.</p>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding-top: 140px;
    padding-bottom: var(--space-3xl);
    background: var(--bg-primary);
  }

  .hero-content {
    max-width: 900px;
    margin: 0 auto;
    text-align: center;
    display: flex;
    flex-direction: column;
  }

  .badge {
    display: inline-block;
    align-self: center;
    width: fit-content;
    padding: var(--space-xs) var(--space-md);
    background: rgba(237, 137, 54, 0.1);
    border: 1px solid rgba(237, 137, 54, 0.3);
    color: var(--orange);
    border-radius: 100px;
    font-size: 0.875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: var(--space-lg);
  }

  .hero h1 {
    margin-bottom: var(--space-lg);
  }

  .text-gradient {
    display: inline-block;
    background: linear-gradient(135deg, var(--orange) 0%, var(--blue) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    color: transparent;
  }

  .hero-subheadline {
    font-size: clamp(1.125rem, 2.5vw, 1.375rem);
    color: var(--text-secondary);
    margin-bottom: var(--space-lg);
    max-width: 750px;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.7;
  }

  .hero-cta {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
    align-items: center;
    margin-bottom: var(--space-md);
  }

  @media (min-width: 480px) {
    .hero-cta {
      flex-direction: row;
      justify-content: center;
    }
  }

  .stats-bar {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-lg);
    padding: var(--space-md) 0;
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    margin-bottom: var(--space-md);
  }

  .stat {
    text-align: center;
  }

  .stat-number {
    display: block;
    font-size: 1.75rem;
    font-weight: 800;
    color: var(--navy);
    line-height: 1;
    margin-bottom: 4px;
  }

  .stat-label {
    font-size: 0.75rem;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    font-weight: 500;
  }

  .trust-signal {
    font-size: 0.9375rem;
    font-style: italic;
    color: var(--text-muted);
  }

  /* Mobile */
  @media (max-width: 767px) {
    .hero {
      padding-top: 90px;
      padding-bottom: var(--space-lg);
      min-height: auto;
      align-items: flex-start;
    }

    .badge { margin-bottom: var(--space-xs); font-size: 0.75rem; padding: 4px var(--space-sm); }
    .hero h1 { margin-bottom: var(--space-xs); }
    .hero-subheadline {
      margin-bottom: var(--space-md);
      font-size: 0.9375rem;
      line-height: 1.5;
    }

    .hero-cta .btn {
      width: 100%;
      justify-content: center;
    }

    .stats-bar {
      gap: var(--space-xs);
      padding: var(--space-sm) 0;
      margin-bottom: var(--space-xs);
    }

    .stat-number {
      font-size: 1.125rem;
      margin-bottom: 2px;
    }

    .stat-label {
      font-size: 0.625rem;
      letter-spacing: 0.04em;
    }

    .trust-signal { font-size: 0.8125rem; }
  }
</style>
