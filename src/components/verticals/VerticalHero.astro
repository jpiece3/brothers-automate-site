---
import EyebrowTag from '../operator/EyebrowTag.astro';
import SpecStrip from '../operator/SpecStrip.astro';
import MonoCTA from '../operator/MonoCTA.astro';
import TopUtilityStrip from '../operator/TopUtilityStrip.astro';

interface Props {
  badge: string;
  headline: string;
  headlineGradient: string;
  subheadline: string;
  stats: { number: string; label: string }[];
  trustSignal: string;
}

const { badge, headline, headlineGradient, subheadline, stats, trustSignal } = Astro.props;
---

<section class="op-vert-hero op-grid-bg" id="hero">
  <div class="op-vert-hero-frame">
    <div class="op-vert-hero-util">
      <TopUtilityStrip
        left={`BROTHERS_AUTOMATE / FOR · ${badge.toUpperCase()}`}
        right="● ACCEPTING NEW BUILDS"
        bordered
      />
    </div>

    <EyebrowTag label={`INDUSTRY · ${badge.toUpperCase()}`} />

    <h1 class="op-vert-h1" data-animate="fade-up">
      {headline}<br />
      <span class="op-vert-h1-orange">{headlineGradient}</span>
    </h1>

    <p class="op-vert-sub" data-animate="fade-up">{subheadline}</p>

    <div class="op-vert-ctas" data-animate="fade-up">
      <MonoCTA label="Talk to the Brothers" variant="orange" size="lg" calLink="james-pinder-simpleflo/free-quiz-audit-automation-brothers" trackLocation={`vertical-hero-${badge}`} trackLabel="Talk to the Brothers" />
      <a href="/projects" class="op-vert-link op-mono">SEE OUR PROJECTS →</a>
    </div>

    {stats && stats.length > 0 && (
      <div class="op-vert-specs" data-animate="fade-up">
        <SpecStrip cells={stats.map((s) => ({ value: s.number, label: s.label }))} />
      </div>
    )}

    <p class="op-vert-trust op-mono">● {trustSignal.toUpperCase()}</p>
  </div>
</section>

<style>
  .op-vert-hero { padding: 120px var(--op-section-px) 64px; color: var(--op-ink); }
  .op-vert-hero-frame { max-width: var(--op-max-content); margin: 0 auto; }
  .op-vert-hero-util { margin-bottom: 32px; }
  .op-vert-hero :global(.op-eyebrow) { margin-bottom: 24px; }
  .op-vert-h1 {
    font-family: var(--op-font-display);
    font-weight: 800;
    font-size: clamp(48px, 6.4vw, 84px);
    line-height: 0.98;
    letter-spacing: var(--op-tracking-display);
    color: var(--op-ink);
    margin: 0 0 24px;
    max-width: 900px;
    text-wrap: balance;
  }
  .op-vert-h1-orange { color: var(--op-orange); }
  .op-vert-sub {
    font-family: var(--op-font-body);
    font-size: 17px;
    line-height: 1.55;
    font-weight: 500;
    color: var(--op-navy-soft);
    margin: 0 0 32px;
    max-width: 700px;
  }
  .op-vert-ctas { display: flex; align-items: center; gap: 24px; flex-wrap: wrap; margin-bottom: 40px; }
  .op-vert-link {
    font-size: 11px;
    color: var(--op-ink);
    text-decoration: none;
    border-bottom: 2px solid var(--op-ink);
    padding-bottom: 4px;
    transition: color var(--op-dur-hover) var(--op-ease), border-color var(--op-dur-hover) var(--op-ease);
  }
  .op-vert-link:hover { color: var(--op-orange); border-color: var(--op-orange); }
  .op-vert-specs { margin-bottom: 24px; }
  .op-vert-trust { font-size: 10px; color: var(--op-navy-soft); margin: 0; }
  @media (max-width: 700px) {
    .op-vert-hero { padding: 100px 22px 56px; }
    .op-vert-ctas { flex-direction: column; align-items: stretch; }
  }
</style>
